<view class="container">
  <view class="header">
    <text class="title">我的相册</text>
    <view class="header-right">
      <text class="count">{{imageList.length}}/{{maxCount}}</text>
      <text class="logout-btn" bindtap="logout">退出</text>
    </view>
  </view>

  <view class="grid" wx:if="{{imageList.length > 0}}">
    <view
      class="grid-item"
      wx:for="{{imageList}}"
      wx:key="index"
      data-index="{{index}}"
      bindtap="previewImage"
      bindlongpress="longPressImage"
    >
      <image mode="aspectFill" src="{{item}}" />
      <view class="delete-icon" catchtap="deleteImage" data-index="{{index}}" wx:if="{{canUpload}}">×</view>
    </view>
  </view>

  <view class="empty" wx:else>
    <text>暂无图片</text>
  </view>

  <view class="btn-box">
    <button class="add-btn" bindtap="chooseImage" wx:if="{{canUpload && imageList.length < maxCount}}">
      + 添加图片
    </button>
    <view class="no-permission" wx:if="{{!canUpload}}">
      <text>仅管理员可上传图片</text>
    </view>
    <button class="clear-btn" bindtap="clearAll" wx:if="{{canUpload && imageList.length > 0}}">
      清空相册
    </button>
  </view>
</view>